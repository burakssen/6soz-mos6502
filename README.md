# 6soz-core

A 6502 CPU core in Zig for use with the 6SOZ emulator project.

## Status
- Focused on CPU core + bus interface.
- Does not include a full emulator (PPU/APU/I-O, cartridge, or host integration).

## Usage
```sh
zig build
zig build test
```
